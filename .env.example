# Production URL of the site, used for the sitemap, robots.txt, canonical URLs
# and Open Graph metadata. Set this in your deployment environment (and in
# .env.local for local testing) once the real domain is available.
SITE_URL=https://www.example.com
